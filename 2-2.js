let min = prompt('Enter a minimal number');
let current = prompt('Enter your current number');
let max = prompt('Enter your maximal number');

if (parseInt(min) > parseInt(max)) {
    window.alert("You really don't understand anything. End of program.");
}